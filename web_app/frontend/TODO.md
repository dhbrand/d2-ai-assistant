# Frontend TODOs

- Fix React Warning: Each child in a list should have a unique "key" prop in `Dashboard.js` where catalysts are mapped.
  - Ensure the `key` prop uses a unique value from the catalyst data (e.g., `catalyst.recordHash`). 